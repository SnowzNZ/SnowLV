name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.2.3 or v1.2.3-beta.1)'
        required: false
        type: string

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  APP_NAME: SnowLV

jobs:
  # Determine if this is a stable or prerelease build
  check-tag:
    name: Check Tag Type
    runs-on: ubuntu-latest
    outputs:
      is_prerelease: ${{ steps.check.outputs.is_prerelease }}
      suffix: ${{ steps.check.outputs.suffix }}
      display_name: ${{ steps.check.outputs.display_name }}
      bundle_id: ${{ steps.check.outputs.bundle_id }}
      dmg_volname: ${{ steps.check.outputs.dmg_volname }}
    steps:
      - name: Determine release type
        id: check
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          if [[ "$TAG" =~ -(beta|alpha|rc) ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
            echo "suffix=-beta" >> $GITHUB_OUTPUT
            echo "display_name=SnowLV (Beta)" >> $GITHUB_OUTPUT
            echo "bundle_id=com.snowlv.app.beta" >> $GITHUB_OUTPUT
            echo "dmg_volname=SnowLV Beta" >> $GITHUB_OUTPUT
            echo "Tag $TAG is a prerelease"
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
            echo "suffix=" >> $GITHUB_OUTPUT
            echo "display_name=SnowLV" >> $GITHUB_OUTPUT
            echo "bundle_id=com.snowlv.app" >> $GITHUB_OUTPUT
            echo "dmg_volname=SnowLV" >> $GITHUB_OUTPUT
            echo "Tag $TAG is a stable release"
          fi

  build-linux:
    name: Build Linux
    needs: check-tag
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            libssl-dev \
            libgtk-3-dev \
            libglib2.0-dev \
            libatk1.0-dev \
            libcairo2-dev \
            libpango1.0-dev \
            libgdk-pixbuf2.0-dev \
            libwayland-dev \
            libxcb1-dev \
            libxcb-randr0-dev \
            libxcb-xkb-dev \
            libx11-xcb-dev \
            libxml2-dev

      - name: Build release binary
        run: cargo build --release --target x86_64-unknown-linux-gnu

      - name: Package Linux binary
        run: |
          mkdir -p output
          cp target/x86_64-unknown-linux-gnu/release/snowlv output/snowlv-linux
          chmod +x output/snowlv-linux
          cd output
          tar -czvf snowlv-linux${{ needs.check-tag.outputs.suffix }}.tar.gz snowlv-linux
          rm snowlv-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: snowlv-linux${{ needs.check-tag.outputs.suffix }}
          path: output/

  build-windows:
    name: Build Windows
    needs: check-tag
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build release binary
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Package Windows binary
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path output
          Copy-Item "target/x86_64-pc-windows-msvc/release/snowlv.exe" -Destination "output/snowlv-windows${{ needs.check-tag.outputs.suffix }}.exe"
          Compress-Archive -Path "output/snowlv-windows${{ needs.check-tag.outputs.suffix }}.exe" -DestinationPath "output/snowlv-windows${{ needs.check-tag.outputs.suffix }}.zip"
          Remove-Item "output/snowlv-windows${{ needs.check-tag.outputs.suffix }}.exe"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: snowlv-windows${{ needs.check-tag.outputs.suffix }}
          path: output/

  build-macos:
    name: Build macOS (${{ matrix.arch }})
    needs: check-tag
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            arch: intel
          - target: aarch64-apple-darwin
            arch: arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Get version
        id: version
        run: echo "VERSION=$(grep '^version' Cargo.toml | head -1 | sed 's/.*\"\(.*\)\".*/\1/')" >> $GITHUB_OUTPUT

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Create app bundle and DMG
        env:
          VERSION: ${{ steps.version.outputs.VERSION }}
          DISPLAY_NAME: ${{ needs.check-tag.outputs.display_name }}
          BUNDLE_ID: ${{ needs.check-tag.outputs.bundle_id }}
          DMG_VOLNAME: ${{ needs.check-tag.outputs.dmg_volname }}
          SUFFIX: ${{ needs.check-tag.outputs.suffix }}
        run: |
          ASSET_NAME="snowlv-macos-${{ matrix.arch }}${SUFFIX}"
          mkdir -p output

          # Create app bundle structure (named just "SnowLV.app")
          APP_DIR="output/${{ env.APP_NAME }}.app"
          mkdir -p "$APP_DIR/Contents/MacOS"
          mkdir -p "$APP_DIR/Contents/Resources"

          # Copy binary
          cp target/${{ matrix.target }}/release/snowlv "$APP_DIR/Contents/MacOS/snowlv"
          chmod +x "$APP_DIR/Contents/MacOS/snowlv"

          # Create Info.plist
          cat > "$APP_DIR/Contents/Info.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleName</key>
              <string>${{ env.APP_NAME }}</string>
              <key>CFBundleDisplayName</key>
              <string>${DISPLAY_NAME}</string>
              <key>CFBundleIdentifier</key>
              <string>${BUNDLE_ID}</string>
              <key>CFBundleVersion</key>
              <string>${VERSION}</string>
              <key>CFBundleShortVersionString</key>
              <string>${VERSION}</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleExecutable</key>
              <string>snowlv</string>
              <key>CFBundleIconFile</key>
              <string>AppIcon</string>
              <key>LSMinimumSystemVersion</key>
              <string>10.15</string>
              <key>NSHighResolutionCapable</key>
              <true/>
              <key>NSSupportsAutomaticGraphicsSwitching</key>
              <true/>
              <key>CFBundleDocumentTypes</key>
              <array>
                  <dict>
                      <key>CFBundleTypeExtensions</key>
                      <array>
                          <string>csv</string>
                          <string>log</string>
                          <string>txt</string>
                          <string>mlg</string>
                      </array>
                      <key>CFBundleTypeName</key>
                      <string>ECU Log File</string>
                      <key>CFBundleTypeRole</key>
                      <string>Viewer</string>
                  </dict>
              </array>
          </dict>
          </plist>
          EOF

          # Create PkgInfo
          echo -n "APPL????" > "$APP_DIR/Contents/PkgInfo"

          # Create icon from PNG if exists
          ICNS_PATH=""
          if [ -f "assets/icons/mac.png" ]; then
            ICONSET_DIR=$(mktemp -d)/AppIcon.iconset
            mkdir -p "$ICONSET_DIR"
            sips -z 16 16     "assets/icons/mac.png" --out "$ICONSET_DIR/icon_16x16.png"
            sips -z 32 32     "assets/icons/mac.png" --out "$ICONSET_DIR/icon_16x16@2x.png"
            sips -z 32 32     "assets/icons/mac.png" --out "$ICONSET_DIR/icon_32x32.png"
            sips -z 64 64     "assets/icons/mac.png" --out "$ICONSET_DIR/icon_32x32@2x.png"
            sips -z 128 128   "assets/icons/mac.png" --out "$ICONSET_DIR/icon_128x128.png"
            sips -z 256 256   "assets/icons/mac.png" --out "$ICONSET_DIR/icon_128x128@2x.png"
            sips -z 256 256   "assets/icons/mac.png" --out "$ICONSET_DIR/icon_256x256.png"
            sips -z 512 512   "assets/icons/mac.png" --out "$ICONSET_DIR/icon_256x256@2x.png"
            sips -z 512 512   "assets/icons/mac.png" --out "$ICONSET_DIR/icon_512x512.png"
            sips -z 1024 1024 "assets/icons/mac.png" --out "$ICONSET_DIR/icon_512x512@2x.png"
            iconutil -c icns "$ICONSET_DIR" -o "$APP_DIR/Contents/Resources/AppIcon.icns"
            ICNS_PATH="$APP_DIR/Contents/Resources/AppIcon.icns"
          fi

          # Ad-hoc sign the app bundle
          codesign --force --deep --sign - "$APP_DIR"

          # Create DMG with volume icon if available
          if [ -n "$ICNS_PATH" ]; then
            create-dmg \
              --volname "${DMG_VOLNAME}" \
              --volicon "$ICNS_PATH" \
              --window-pos 200 120 \
              --window-size 600 400 \
              --icon-size 100 \
              --icon "${{ env.APP_NAME }}.app" 150 190 \
              --hide-extension "${{ env.APP_NAME }}.app" \
              --app-drop-link 450 190 \
              "output/${ASSET_NAME}.dmg" \
              "$APP_DIR" || \
              hdiutil create -volname "${DMG_VOLNAME}" -srcfolder "$APP_DIR" -ov -format UDZO "output/${ASSET_NAME}.dmg"
          else
            create-dmg \
              --volname "${DMG_VOLNAME}" \
              --window-pos 200 120 \
              --window-size 600 400 \
              --icon-size 100 \
              --icon "${{ env.APP_NAME }}.app" 150 190 \
              --hide-extension "${{ env.APP_NAME }}.app" \
              --app-drop-link 450 190 \
              "output/${ASSET_NAME}.dmg" \
              "$APP_DIR" || \
              hdiutil create -volname "${DMG_VOLNAME}" -srcfolder "$APP_DIR" -ov -format UDZO "output/${ASSET_NAME}.dmg"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: snowlv-macos-${{ matrix.arch }}${{ needs.check-tag.outputs.suffix }}
          path: output/*.dmg

  release:
    name: Create Release
    needs: [check-tag, build-linux, build-windows, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Get tag name
        id: tag
        run: echo "TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure
        run: |
          echo "Artifacts structure:"
          ls -laR artifacts/

      - name: Create stable release
        if: needs.check-tag.outputs.is_prerelease == 'false'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/snowlv-macos-intel/*.dmg
            artifacts/snowlv-macos-arm64/*.dmg
            artifacts/snowlv-windows/*.zip
            artifacts/snowlv-linux/*.tar.gz
          draft: false
          prerelease: false
          generate_release_notes: true
          body_path: .github/RELEASE_TEMPLATE.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create prerelease
        if: needs.check-tag.outputs.is_prerelease == 'true'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/snowlv-macos-intel-beta/*.dmg
            artifacts/snowlv-macos-arm64-beta/*.dmg
            artifacts/snowlv-windows-beta/*.zip
            artifacts/snowlv-linux-beta/*.tar.gz
          draft: false
          prerelease: true
          name: "${{ steps.tag.outputs.TAG }} (Beta)"
          generate_release_notes: true
          body: |
            ## Beta Release

            **This is a pre-release version for testing purposes.**

            This build may contain experimental features, bugs, or incomplete functionality.
            Please report any issues you encounter on [GitHub Issues](https://github.com/${{ github.repository }}/issues).

            ---

            ## Installation

            ### macOS
            1. Download the appropriate `.dmg` file for your Mac:
               - **Intel Mac**: `snowlv-macos-intel-beta.dmg`
               - **Apple Silicon (M1/M2/M3/M4)**: `snowlv-macos-arm64-beta.dmg`
            2. Open the DMG and drag SnowLV to your Applications folder
            3. On first run, right-click the app and select "Open" to bypass Gatekeeper

            ### Windows
            1. Download `snowlv-windows-beta.zip`
            2. Extract the zip file
            3. Run `snowlv-windows-beta.exe`

            ### Linux
            1. Download `snowlv-linux-beta.tar.gz`
            2. Extract: `tar -xzf snowlv-linux-beta.tar.gz`
            3. Run: `./snowlv-linux`

            ---

            **Note:** Beta releases are not shown by default in the releases list.
            Users must explicitly opt-in to view pre-releases.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
